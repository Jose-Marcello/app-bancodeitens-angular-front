{
  "routes": [
    {
      "route": "/api/*",
      "methods": ["GET", "POST", "PUT", "DELETE"],
      "backend": "app-bancodeitens-api" 
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html"
  }
}